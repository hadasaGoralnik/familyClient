import { UpdateUserRequest } from './update-user-request';

describe('UpdateUserRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateUserRequest()).toBeTruthy();
  });
});
