import { AddUeserToGroupRequest } from './add-ueser-to-group-request';

describe('AddUeserToGroupRequest', () => {
  it('should create an instance', () => {
    expect(new AddUeserToGroupRequest()).toBeTruthy();
  });
});
